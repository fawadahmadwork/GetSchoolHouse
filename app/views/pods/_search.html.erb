
<%= form_tag(pods_path, method: :get, class: "search-form") do %>
  <%= text_field_tag(:search, params[:search], placeholder: "Search by name, address, zip_code, or grade '1 to 5'") %>
  <%= submit_tag("Search") %>
<% end %>