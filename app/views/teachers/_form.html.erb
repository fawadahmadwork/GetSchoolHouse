<%= form_with(model: teacher) do |form| %>
  <% if teacher.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(teacher.errors.count, "error") %> prohibited this teacher from being saved:</h2>

      <ul>
        <% teacher.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :qualififcation, style: "display: block" %>
    <%= form.text_field :qualififcation %>
  </div>

  <div>
    <%= form.label :grade, style: "display: block" %>
    <%= form.select :grade, options_for_select((1..5).map { |n| [n.ordinalize, n] }) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
