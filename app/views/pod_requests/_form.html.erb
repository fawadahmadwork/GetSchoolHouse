<%= form_with(model: pod_request) do |form| %>
  <% if pod_request.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pod_request.errors.count, "error") %> prohibited this pod_request from being saved:</h2>

      <ul>
        <% pod_request.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :child_id, style: "display: block" %>
    <%= form.text_field :child_id %>
  </div>

  <div>
    <%= form.label :pod_id, style: "display: block" %>
    <%= form.text_field :pod_id %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.text_field :status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
