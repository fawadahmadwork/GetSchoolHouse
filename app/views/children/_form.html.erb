<%= form_with(model: child) do |form| %>
  <% if child.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(child.errors.count, "error") %> prohibited this child from being saved:</h2>

      <ul>
        <% child.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :age, style: "display: block" %>
    <%= form.number_field :age, placeholder: 'select from 6 to 13' %>
  </div>

  <div>
    <%= form.label :grade, style: "display: block" %>
     <%= form.select :grade, options_for_select((1..5).map { |n| [n.ordinalize, n] }) %>
  </div><br>
  <div>
    <%= form.submit %>
  </div>
<% end %>
